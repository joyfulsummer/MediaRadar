# This file keeps list of dependencies required for the Elasticsearch python client, Youtube API client, and Flask
# install package for Youtube API scraping 
pip install pafy
pip install --upgrade google-api-python-client
pip install --upgrade oauth2client
sudo pip install --upgrade youtube_dl

# install aws client
sudo apt install awscli

# install elasticsearch
pip install "elasticsearch-dsl>=6.0.0"
pip install boto3

# install flask 
pip install flask
